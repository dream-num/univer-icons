{
  "name": "@univerjs/icons-workspace",
  "version": "1.6.0",
  "private": true,
  "packageManager": "pnpm@11.5.1",
  "description": "Icons used by Univer",
  "author": "DreamNum Co., Ltd. <developer@univer.ai>",
  "license": "MIT",
  "repository": "https://github.com/dream-num/univer-icons.git",
  "sideEffects": false,
  "main": "index.js",
  "devEngines": {
    "runtime": [
      {
        "name": "node",
        "range": ">=22.18"
      },
      {
        "name": "pnpm",
        "range": ">=11"
      }
    ]
  },
  "scripts": {
    "build": "pnpm --filter @univerjs/icons-svg run prepare && pnpm --filter ./packages/icons run build && pnpm --filter ./packages/icons-vue run build",
    "dev": "pnpm build && pnpm --filter ./packages/app dev",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "lint:staged": "lint-staged",
    "prepare": "simple-git-hooks"
  },
  "devDependencies": {
    "@types/node": "^25.9.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "lint-staged": "^17.0.5",
    "oxfmt": "^0.53.0",
    "oxlint": "^1.66.0",
    "simple-git-hooks": "^2.13.1",
    "typescript": "^6.0.3"
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm lint:staged"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,cjs,cts,mjs,mts}": "oxlint",
    "*.{md,markdown,yml,yaml}": "oxfmt --check ."
  }
}
